using Microsoft.AspNetCore.Builder;
using Microsoft.AspNetCore.Hosting;
using Microsoft.AspNetCore.HttpsPolicy;
using Microsoft.AspNetCore.Mvc;
using Microsoft.AspNetCore.SpaServices.ReactDevelopmentServer;
using Microsoft.Extensions.Configuration;
using Microsoft.Extensions.DependencyInjection;
using StringsServer.Repository.FileSystem;
using StringsServer.Contracts.Models;
using StringsServer.Contracts.Repository;
using System;
using AutoMapper;
using System.Collections.Generic;
using StringsServer.Models;

namespace StringsServer
{
    public class Startup
    {
        public Startup(IConfiguration configuration)
        {
            Configuration = configuration;
        }

        public IConfiguration Configuration { get; }

        // This method gets called by the runtime. Use this method to add services to the container.
        public void ConfigureServices(IServiceCollection services)
        {
            services.AddMvc().SetCompatibilityVersion(CompatibilityVersion.Version_2_1);

            // In production, the React files will be served from this directory
            services.AddSpaStaticFiles(configuration =>
            {
                configuration.RootPath = "ClientApp/build";
            });

            // TODO: settings
            var filepath = @"c:/tst/tstrecords.json"; // read from settings 
            services.Add(ServiceDescriptor.Singleton(typeof(IRepository<StringMessage>), new FileSystemRepository<StringMessage>(filepath)));
        }

        // This method gets called by the runtime. Use this method to configure the HTTP request pipeline.
        public void Configure(IApplicationBuilder app, IHostingEnvironment env)
        {
            if (env.IsDevelopment())
            {
                app.UseDeveloperExceptionPage();
            }
            else
            {
                app.UseExceptionHandler("/Error");
                app.UseHsts();
            }

            //app.UseHttpsRedirection();
            app.UseStaticFiles();
            app.UseSpaStaticFiles();

            // MVC? 
            app.UseMvc(routes =>
            {
                routes.MapRoute(
                    name: "default",
                    template: "{controller}/{action=Index}/{id?}");
            });

            app.UseSpa(spa =>
            {
                spa.Options.SourcePath = "ClientApp";

                if (env.IsDevelopment())
                {
                    spa.UseReactDevelopmentServer(npmScript: "start");
                }
            });

            ConfigureAutoMapper();
        }

        private static void ConfigureAutoMapper()
        {
            Mapper.Initialize(cfg => 
            {
                cfg.CreateMap<StringMessage, StringMessageModel>();

                 
            });

            Mapper.AssertConfigurationIsValid(); // CreateMap<Book, BookViewModel>();
        }
    }
}
